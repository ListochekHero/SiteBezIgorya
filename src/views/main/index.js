import {Typography} from "@mui/material";
import {MainContainer} from "../../components/UI/mainContainer";

export const Main = () => {
    return (
        <MainContainer>
            <Typography>
                MAIN
            </Typography>
        </MainContainer>
    );
};